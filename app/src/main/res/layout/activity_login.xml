<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    xmlns:app="http://schemas.android.com/apk/res-auto">


<LinearLayout
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintVertical_bias="0.3"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context=".login.view.LoginActivity">

    <ImageView
        android:id="@+id/login_img_logo"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginBottom="48dp"
        android:contentDescription="@null"
        android:src="@drawable/instagram_logo_black" />

    <EditText
        android:id="@+id/login_edit_email"
        android:hint="@string/email"
        android:inputType="textEmailAddress"
        android:autofillHints="@null"
        style="@style/Theme.InstagranPortifólio.FormEditText"/>
    <EditText
    android:id="@+id/login_edit_senha"
    android:hint="@string/email"
    android:inputType="textPassword"
    android:autofillHints="@null"
    style="@style/Theme.InstagranPortifólio.FormEditText"/>

    <Button
        android:id="@+id/login_btn_enter"
        style="@style/Theme.InstagranPortifólio.Button"
        android:text="@string/enter"
        />

    <FrameLayout
        style="@style/Theme.InstagranPortifólio.FormContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_gravity="center"
            android:background="@color/gray_border"/>
        
        <TextView
            style="@style/Theme.InstagranPortifólio.TextViewBase"
           android:text="@string/or"
            android:layout_gravity="center"
            android:background="@color/white"
            android:padding="8dp"/>

    </FrameLayout>


</LinearLayout>

    <View
        app:layout_constraintBottom_toTopOf="@+id/login_txt_register"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@color/gray_darker"
        android:layout_width="match_parent"
        android:layout_height="1dp"/>

    <TextView
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="24dp"
        android:layout_width="match_parent"
        android:textAlignment="center"
        android:text="@string/does_not_have_account"
        android:id="@+id/login_txt_register"
        style="@style/Theme.InstagranPortifólio.TextViewBase"
       />

</androidx.constraintlayout.widget.ConstraintLayout>